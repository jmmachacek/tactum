[package]
name = "tactum"
version = "0.1.0"
edition = "2024"
description = "A minimal macOS desktop-control MVP"
license = "MIT OR Apache-2.0"

[target.'cfg(target_os = "macos")'.dependencies]
core-graphics = "0.25"
image = { version = "0.25", default-features = false, features = ["png"] }
